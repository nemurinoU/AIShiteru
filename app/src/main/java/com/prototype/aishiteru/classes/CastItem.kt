package com.prototype.aishiteru.classes

class CastItem (name: String, lvl: Int, imageId : Int, userId : String) {
    var name = name
        private set

    var lvl = lvl
        private set

    var imageId = imageId
        private set

    var userId = userId
        private set
}